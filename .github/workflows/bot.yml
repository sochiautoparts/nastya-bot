name: Nastya Bot — MULTI-MODEL v43

# ═══════════════════════════════════════════════════════════════
# Nastya Bot v43.0 — MULTI-MODEL POLLINATIONS + VISION + LOCAL FALLBACK!
#
# Architecture:
#   - Pollinations.ai MULTI-MODEL (7 chat models, load balanced!)
#     - openai (GPT-5.4 Nano) PRIMARY — fast, vision-capable
#     - mistral, deepseek, llama, gemma — BACKUP models
#     - Automatic failover on 429/timeout
#   - Qwen3-4B-Instruct GGUF as LOCAL FALLBACK — offline reserve
#   - REAL PHOTO UNDERSTANDING via Pollinations vision API!
#   - Typing delay indicators — human-like behavior
#   - Group chat message length limiting
#   - Proactive messaging — Настя активный собеседник
#   - /no_think + stop=["<think"] for Qwen3 fallback
#   - AVX2 acceleration for local model
#   - RSS + template commentary for news (NO AI for news!)
#   - Single concurrency group — ONE bot instance only!
#
# Schedule (UTC, Moscow = UTC+3):
#   Every 5 hours: 00:00, 05:00, 10:00, 15:00, 20:00
# ═══════════════════════════════════════════════════════════════

on:
  schedule:
    - cron: '0 0 * * *'      # 00:00 UTC = 03:00 Moscow
    - cron: '0 5 * * *'      # 05:00 UTC = 08:00 Moscow
    - cron: '0 10 * * *'     # 10:00 UTC = 13:00 Moscow
    - cron: '0 15 * * *'     # 15:00 UTC = 18:00 Moscow
    - cron: '0 20 * * *'     # 20:00 UTC = 23:00 Moscow
  workflow_dispatch:

# ═══ CRITICAL: Single concurrency group ═══
concurrency:
  group: nastya-bot-single
  cancel-in-progress: true

permissions:
  contents: read
  actions: write

env:
  PYTHONUNBUFFERED: "1"

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 345  # ~5h45m

    steps:
      # ── Kill conflicting runs ──
      - name: Cancel conflicting runs and wait
        env:
          GH_TOKEN: ${{ secrets.GH_PAT_TOKEN }}
        run: |
          echo "=== Checking for other running bot instances ==="
          CURRENT_RUN=${{ github.run_id }}
          REPO=${{ github.repository }}

          for status in in_progress queued; do
            RUNS=$(curl -s -H "Authorization: token $GH_TOKEN" \
              "https://api.github.com/repos/$REPO/actions/runs?status=$status&per_page=10" \
              | python3 -c '
          import sys, json
          try:
              d = json.load(sys.stdin)
              for r in d.get("workflow_runs", []):
                  if r["id"] != int(sys.argv[1]):
                      print(r["id"])
          except: pass
          ' "$CURRENT_RUN")

            for run_id in $RUNS; do
              echo "Cancelling $status run: $run_id"
              curl -s -X POST -H "Authorization: token $GH_TOKEN" \
                -H "Accept: application/vnd.github.v3+json" \
                "https://api.github.com/repos/$REPO/actions/runs/$run_id/cancel" || true
            done
          done

          echo "Waiting 60s for old bot instances to fully stop..."
          sleep 60
          echo "=== Ready to start ==="

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      # ── Install llama-cpp-python with AVX2 acceleration ──
      - name: Install llama-cpp-python with AVX2
        run: |
          echo "=== Installing llama-cpp-python with AVX2 support ==="
          CMAKE_ARGS="-DGGML_AVX2=on" pip install llama-cpp-python --force-reinstall --no-cache-dir 2>&1 || {
            echo "WARNING: AVX2 build failed, trying without..."
            pip install llama-cpp-python --force-reinstall --no-cache-dir
          }
          python3 -c "from llama_cpp import Llama; print('llama-cpp-python OK!')"

      # ── Download LOCAL FALLBACK model: Qwen3-4B-Instruct ──
      - name: Cache LOCAL FALLBACK model (Qwen3-4B)
        id: cache-model
        uses: actions/cache@v4
        with:
          path: models/Qwen3-4B-Instruct-2507-Q4_K_M.gguf
          key: qwen3-4b-instruct-q4km-v3

      - name: Download Qwen3-4B-Instruct GGUF model
        if: steps.cache-model.outputs.cache-hit != 'true'
        run: |
          echo "=== Downloading Qwen3-4B-Instruct Q4_K_M model (~2.4GB) — LOCAL FALLBACK ==="
          mkdir -p models
          python3 -c "
          from huggingface_hub import hf_hub_download
          path = hf_hub_download(
              repo_id='unsloth/Qwen3-4B-Instruct-2507-GGUF',
              filename='Qwen3-4B-Instruct-2507-Q4_K_M.gguf',
              local_dir='models',
          )
          print('Downloaded to:', path)
          "

      - name: Verify model
        run: |
          echo "=== Verifying GGUF model ==="
          MODEL_FILE=models/Qwen3-4B-Instruct-2507-Q4_K_M.gguf
          if [ ! -f "$MODEL_FILE" ] || [ ! -s "$MODEL_FILE" ]; then
            echo "WARNING: Model file missing or empty — running in CLOUD-ONLY mode"
          else
            MODEL_SIZE=$(du -h "$MODEL_FILE" | cut -f1)
            echo "OK: Qwen3-4B-Instruct-Q4_K_M (${MODEL_SIZE}) — LOCAL FALLBACK"
          fi

      # ── Test Pollinations API (v43: multi-model!) ──
      - name: Test Pollinations API connectivity
        run: |
          echo "=== Testing Pollinations.ai API (MULTI-MODEL) ==="
          # Test primary model
          echo "Testing PRIMARY model (openai)..."
          RESPONSE=$(curl -s --max-time 30 https://gen.pollinations.ai/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.POLLINATIONS_API_KEY }}" \
            -d '{"model": "openai", "messages": [{"role": "user", "content": "Скажи ОК"}], "max_tokens": 5, "reasoning_effort": "none", "stream": false}' 2>&1)
          echo "Primary Response: $(echo "$RESPONSE" | head -3)"
          if echo "$RESPONSE" | grep -q '"content"'; then
            echo "PRIMARY model (openai) OK!"
          else
            echo "WARNING: PRIMARY model failed"
          fi

          # Test backup model
          echo "Testing BACKUP model (mistral)..."
          RESPONSE2=$(curl -s --max-time 30 https://gen.pollinations.ai/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.POLLINATIONS_API_KEY }}" \
            -d '{"model": "mistral", "messages": [{"role": "user", "content": "Say OK"}], "max_tokens": 5, "stream": false}' 2>&1)
          echo "Backup Response: $(echo "$RESPONSE2" | head -3)"
          if echo "$RESPONSE2" | grep -q '"content"'; then
            echo "BACKUP model (mistral) OK!"
          else
            echo "WARNING: BACKUP model failed"
          fi

      # ── Test Pollinations Vision API ──
      - name: Test Pollinations Vision API
        run: |
          echo "=== Testing Pollinations.ai Vision API ==="
          TEST_IMAGE="iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg=="
          RESPONSE=$(curl -s --max-time 30 https://gen.pollinations.ai/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.POLLINATIONS_API_KEY }}" \
            -d "{\"model\": \"openai\", \"messages\": [{\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"What color is this image?\"}, {\"type\": \"image_url\", \"image_url\": {\"url\": \"data:image/png;base64,${TEST_IMAGE}\"}}]}], \"max_tokens\": 10, \"stream\": false}" 2>&1)
          echo "Vision Response: $(echo "$RESPONSE" | head -3)"
          if echo "$RESPONSE" | grep -q '"content"'; then
            echo "Pollinations Vision API OK!"
          else
            echo "WARNING: Pollinations Vision API test failed — photo understanding may not work"
          fi

      - name: Create data directory
        run: mkdir -p data

      - name: Restore database cache
        uses: actions/cache@v4
        with:
          path: data/nastya.db
          key: nastya-db-v43
          restore-keys: |
            nastya-db-

      # ── Run the Bot — UNLIMITED AUTO-RESTART ──
      - name: Run Nastya Bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          OWNER_ID: ${{ secrets.OWNER_ID }}
          ADMIN_IDS: ${{ secrets.OWNER_ID }}
          BOT_USERNAME: asnastya_bot
          # Pollinations.ai — MULTI-MODEL PRIMARY + VISION
          POLLINATIONS_API_KEY: ${{ secrets.POLLINATIONS_API_KEY }}
          # Qwen3-4B — LOCAL FALLBACK
          MODEL_PATH: models/Qwen3-4B-Instruct-2507-Q4_K_M.gguf
          MODEL_N_CTX: 2048
          MODEL_N_THREADS: 4
          MODEL_MAX_TOKENS: 256
          MODEL_HISTORY_LIMIT: 10
          GH_PAT_TOKEN: ${{ secrets.GH_PAT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
          CHANNEL_USERNAME: ${{ secrets.CHANNEL_USERNAME }}
          DB_PATH: data/nastya.db
          LOG_LEVEL: INFO
          NEWS_FETCH_INTERVAL: 900
          CHANNEL_POST_INTERVAL: 1200
        run: |
          echo "=== Starting Nastya Bot v43.0 (MULTI-MODEL POLLINATIONS + VISION + QWEN3 FALLBACK) ==="
          echo "PRIMARY: Pollinations.ai MULTI-MODEL (7 models: openai, mistral, deepseek, llama, gemma +2)"
          echo "REASONING: Pollinations.ai (openai-large / GPT-5.4)"
          echo "FALLBACK: Qwen3-4B-Instruct-Q4_K_M (local GGUF)"
          echo "Config: vision=yes, reasoning=openai-large, max_tokens=1000(cloud)/256(local), history=10, load_balancing=yes"
          ls -lh models/*.gguf 2>/dev/null || echo "WARNING: No GGUF models — CLOUD-ONLY mode!"
          echo "=== Launching bot with UNLIMITED auto-restart ==="

          # UNLIMITED retries — bot MUST always be running!
          RETRY=0
          while true; do
            RETRY=$((RETRY+1))
            echo "--- Attempt $RETRY ---"

            python3 -m bot.main
            EXIT_CODE=$?
            echo "Bot exited with code $EXIT_CODE (attempt $RETRY)"

            # Intelligent backoff based on exit code
            if [ $EXIT_CODE -eq 2 ]; then
              echo "Persistent conflicts — restarting in 10s..."
              sleep 10
            elif [ $EXIT_CODE -eq 3 ]; then
              echo "Health check failure — restarting in 20s..."
              sleep 20
            elif [ $EXIT_CODE -eq 0 ]; then
              echo "Session timeout — restarting in 5s..."
              sleep 5
            else
              if [ $RETRY -le 3 ]; then
                SLEEP=10
              elif [ $RETRY -le 6 ]; then
                SLEEP=15
              else
                SLEEP=30
              fi
              echo "Bot crashed (code $EXIT_CODE) — restarting in ${SLEEP}s..."
              sleep $SLEEP
            fi
          done

      - name: Save database cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: data/nastya.db
          key: nastya-db-v43-${{ github.run_id }}

      # ── Self-dispatch for 24/7 ──
      - name: Re-dispatch workflow for continuous operation
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GH_PAT_TOKEN }}
        run: |
          echo "=== Re-dispatching workflow for 24/7 uptime ==="
          REPO=${{ github.repository }}
          sleep 10
          for i in 1 2 3; do
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
              -H "Authorization: token $GH_TOKEN" \
              -H "Accept: application/vnd.github.v3+json" \
              "https://api.github.com/repos/$REPO/actions/workflows/bot.yml/dispatches" \
              -d '{"ref":"main"}')
            if [ "$HTTP_CODE" = "204" ]; then
              echo "Workflow re-dispatched successfully (attempt $i)"
              break
            fi
            echo "Re-dispatch attempt $i failed (HTTP $HTTP_CODE), retrying in 10s..."
            sleep 10
          done
          echo "=== Re-dispatch complete ==="

      - name: Cleanup
        if: always()
        run: |
          echo "=== Bot session ended ==="
